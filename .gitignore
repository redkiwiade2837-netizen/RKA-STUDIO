# OS junk files that can appear inside the Projects/Furniture/Items photo folders
.DS_Store
Thumbs.db
desktop.ini

# Claude Code local session settings (machine-specific, not app config)
.claude/settings.local.json
